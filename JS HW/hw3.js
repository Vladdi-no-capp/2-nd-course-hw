// Existing riddle (preserved)
let answer = String(prompt('Зимой и летом одним цветом. Что это?'));

if (answer.toLowerCase() === 'ёлка' || answer.toLowerCase() === 'елка' || answer.toLowerCase() === 'ель') {
        console.log('Ответ верный! Молодец!');
} else {
        console.log('Ответ неверный. Попробуйте еще раз.');
}

// ===========================
// Задание 1: вывести "Привет" дважды
console.log('Привет');
console.log('Привет');

// Задание 2: вывести цифры от 1 до 5
for (let i = 1; i <= 5; i++) {
    console.log(i);
}

// Задание 3: вывести числа от 7 до 22
for (let i = 7; i <= 22; i++) {
    console.log(i);
}

// Задание 4: объект с зарплатами и вывод через for-in
const obj = {
    "Коля": '200',
    "Вася": '300',
    "Петя": '400'
};

for (let name in obj) {
    console.log(name + ' — зарплата ' + obj[name] + ' долларов.');
}

// Задание 5: делим n=1000 на 2, пока результат не станет < 50
let n = 1000;
let num = 0;
while (n >= 50) {
    n = n / 2;
    num++;
}
console.log('Результат после делений:', n);
console.log('Количество итераций (num):', num);

// Задание 6: вывод напоминаний для каждой пятницы месяца (31 день)
// Укажите номер первой пятницы месяца (число от 1 до 7)
let firstFriday = 6; // измените при необходимости
for (let day = firstFriday; day <= 31; day += 7) {
    console.log(`Сегодня пятница, ${day}-е число. Необходимо подготовить отчет.`);
}